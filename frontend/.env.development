# 개발 서버(npm run dev)에서만 사용. 백엔드(FastAPI)를 절대 URL로 호출.
# 운영 빌드(npm run build)에선 이 값을 비워 두어 API="" → 같은 출처 상대경로로 동작.
# 백엔드 CORS 가 dev origin(http://localhost:5173)을 허용해야 함(현재 allow_origins=["*"] 라 dev 통과).
VITE_API_BASE=http://localhost:8000
