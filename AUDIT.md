# 전체 감사 리포트 (v1.166 · 2026-06-28)

> 전략·코드·방향성·서비스를 실제 코드 + 실구동으로 점검한 결과. 왜곡 없이 사실만.

## 종합 판정
- **기술 완성도**: 출시 가능 수준. 검증 3층(정적/단위 147/통합 e2e 17) 전부 통과, 앱 실부팅 확인.
- **출시 가부**: **제한 베타 가능 / 정식 출시는 3개 선결**(①PostgreSQL 전환 ②실사용 검증 ③시설 데이터).

## ① 전략 — 양호
- 3층 해자 명확: **이해상충 없음(중개·광고 0 → '말릴 수 있는 앱')** / **창끝=전입자 온보딩** / **복리=주민 인증 데이터**. 대형 앱이 구조적으로 못 하는 영역(전세가율 경고·호가 검증·급매 포착)을 정면에 배치.
- 청주 특화 4축(호재·직주근접·육아·전세가율)이 실수요 3대 질문(왜 여기/어디 살아야/지금 사도 되나)에 정합.

## ② 코드 — 양호(+ 개선점)
- **확장성**: 지역코드 `region_codes.py` 단일 소스 ✅. 하드코딩 43111~4는 폴백·픽스처·주석뿐(드리프트 없음). 충북 확장 = 설정 추가 수준.
- **성능**: 사용자 경로 N+1 없음(게시판·통근검색 모두 `.in_()` 배치). stat_cached 26개 함수 캐싱. data_version 무효화.
- **안정성**: 부팅 실패·캐시 전이 등 과거 사고를 정적 게이트(verify_all)+e2e로 차단. 
- **개선 필요**: main.jsx 4,751줄 단일 파일(분리), 프런트 GU_NAME 중복.

## ③ 왜곡(distortion) — 우수
- 모든 모의데이터 `is_sample=True`+`source=FIXTURE/예시` 격리. 빈 DB에서 시세·호가·단지상세가 값을 지어내지 않고 `found=False`/`부족 안내`/면책 반환(실행으로 확인). 공공데이터 출처 표기.

## ④ 서비스/운영 — 조건부
- 배포 인프라(Docker·render.yaml·preflight·doctor·자동수집 크론) 갖춤. 법적 고지·개인정보처리방침·약관·동의이력 갖춤.
- **치명 리스크**: 운영이 SQLite면 재배포 시 데이터 소실 → **PostgreSQL 전환 필수**(OPERATIONS §7).

## ⑤ 문서 — 인수인계 가능
- CLAUDE(구조·함정)·ROADMAP(SSOT 스냅샷 v1.166)·OPERATIONS·DATA·DATA_PLACES·TESTING(3층 검증)·skills/* 최신. CHANGELOG 전 버전 이력.

## 선결 과제(출시 전, 우선순위)
1. **PostgreSQL 전환** + `doctor`로 데이터 영속 확인 (최우선·운영)
2. **시설 데이터 적재**(NEIS 학원 키) → 육아·초품아·지도 POI 활성
3. **지인 5~10명 실사용** → 브라우저 스모크 + 치명 버그 제거
4. main.jsx 컴포넌트 분리(사용자 증가 전 안정화)
